import React from "react"

type AnswerProps = {}

export const AnswerComponent: React.FC<AnswerProps> = (props: AnswerProps) => (
    <React.Fragment>
        <span>Some answer</span>
    </React.Fragment>
)
