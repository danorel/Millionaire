import React from "react"

type QuestionProps = {}

export const QuestionComponent: React.FC<QuestionProps> = (
    props: QuestionProps
) => (
    <React.Fragment>
        <span>Some question</span>
    </React.Fragment>
)
